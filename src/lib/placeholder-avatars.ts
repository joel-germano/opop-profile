// Rostos placeholder (i.pravatar.cc) só pra ilustrar o visual de "quem
// apoia" enquanto a tela ainda não tem apoiadores reais o bastante pra
// preencher a fileira — usado na home e na home dos presidenciáveis.
export const PLACEHOLDER_SUPPORTER_AVATARS = [12, 24, 33, 47, 58, 5, 19, 41, 52, 65].map(
  (seed) => `https://i.pravatar.cc/80?img=${seed}`
);
